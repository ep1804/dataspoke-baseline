# ==============================================================================
# DataSpoke Environment Variables
# ==============================================================================
# Naming convention:
#   DATASPOKE_          — shared across all environments
#   DATASPOKE_DEV_      — local development only
#   KUBE in name        — Kubernetes resource related
# ==============================================================================

# --- Kubernetes (shared) -----------------------------------------------------
# Cluster context — run `kubectl config get-contexts` to list available contexts.
DATASPOKE_KUBE_CLUSTER=minikube

# Namespace names — rename freely before running install.sh.
DATASPOKE_KUBE_DATAHUB_NAMESPACE=datahub-01
DATASPOKE_KUBE_DATASPOKE_NAMESPACE=dataspoke-team1

# --- Kubernetes / Dev only ----------------------------------------------------
# Dummy data namespace for example data sources
DATASPOKE_DEV_KUBE_DUMMY_DATA_NAMESPACE=dummy-data1

# example-postgres credentials (dev only)
DATASPOKE_DEV_KUBE_DUMMY_DATA_POSTGRES_USER=postgres
DATASPOKE_DEV_KUBE_DUMMY_DATA_POSTGRES_PASSWORD=ExampleDev2024!
DATASPOKE_DEV_KUBE_DUMMY_DATA_POSTGRES_DB=example_db
DATASPOKE_DEV_KUBE_DUMMY_DATA_POSTGRES_PORT_FORWARD_PORT=5432

# DataHub Helm chart versions
DATASPOKE_DEV_KUBE_DATAHUB_PREREQUISITES_CHART_VERSION=0.2.1
DATASPOKE_DEV_KUBE_DATAHUB_CHART_VERSION=0.8.3

# DataHub port-forward
DATASPOKE_DEV_KUBE_DATAHUB_PORT_FORWARD_UI_PORT=9002

# DataHub MySQL credentials (dev only)
# Generate secure passwords: 16+ chars, mixed case, at least one special character.
# Example: openssl rand -base64 16 | tr '+/' '-_'
DATASPOKE_DEV_KUBE_DATAHUB_MYSQL_ROOT_PASSWORD=<replace-with-secure-password>
DATASPOKE_DEV_KUBE_DATAHUB_MYSQL_PASSWORD=<replace-with-secure-password>
